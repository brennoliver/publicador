# Senhas em bcrypt hash — gere com: node app/scripts/hash-password.js <senha>
BRENNO_PASS=$2a$12$...hash_aqui...
BIANCA_PASS=$2a$12$...hash_aqui...

# Chave secreta de sessão — gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=troque_por_uma_string_aleatoria_longa

# Ambiente
NODE_ENV=production
PORT=3737
